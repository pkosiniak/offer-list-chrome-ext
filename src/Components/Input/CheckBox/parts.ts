import styled from 'styled-components';
import Box from '../../Box/Box';
import { Label, StyledInput } from '../parts';

export const CheckBoxWrapper = styled(Box)`
   
`;

export const CheckBox = styled(StyledInput)`
   
`;

export { Label };